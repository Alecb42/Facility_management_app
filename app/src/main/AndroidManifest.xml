<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.babychka.facility">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Concordia Facility Management"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".EmployeeActivity" />
        <activity android:name=".ManagerActivity" />
        <activity android:name=".InventoryActivity" />
        <activity android:name=".firstFloorBathroom" />
        <activity android:name=".CleanActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".BathroomActivity" />
        <activity android:name=".EntryHistoryActivity" />
        <activity android:name=".Calendar_Activity" />
        <activity android:name=".WorkHistoryActivity" />
        <activity android:name=".workRecordDisplay"></activity>
    </application>

</manifest>